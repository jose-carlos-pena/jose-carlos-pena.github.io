let canvas;
let select;
let askButton;
let check;

function setup(){
  canvas = createCanvas(windowWidth, windowHeight);
  background(160);

  select = createSelect();
  select.option("One");
  select.option("Two")
}

function draw(){

}
